#include <stdio.h>
#define MAX_BARALHO 13

int main(){
    int i;
    char cartas[52];
    int somaP = 0, somaC = 0, somaO = 0, somaE = 0;
    int arrayP[13];

    fgets(cartas, 52, stdin);

    for(i = 0; cartas != '\0'; i++){
        if(cartas[i] == 'P'){
            
        }
    }
  
    return 0;
}